# Funkcija main

# Preveri, ali je bilo podano pravilno število argumentov. Če ne, izpiše uporabo programa in vrne nenormalni izhod.
# Odpri vhodno datoteko, ki je podana kot prvi argument programa. Če datoteke ni mogoče odpreti, izpiše napako in vrne nenormalni izhod.
# Preberi vhodne podatke iz datoteke in jih shranite v vektor unsigned char.
# Zapri vhodno datoteko.
# Izvedi binarni Radix sort na vhodnih podatkih, shranjenih v vektorju.
# Odpre izhodno datoteko "out.txt". Če datoteke ni mogoče ustvariti, izpiše napako in vrne nenormalni izhod.
# Zapiše sortirane številke iz vektorja v izhodno datoteko, ločene s presledki.
# Zapre izhodno datoteko.
# Izhodna sporočila potrdijo, da je sortiranje uspešno zaključeno in rezultat zapisan v "out.txt".
# Vrne normalni izhod, kar označuje uspešen zaključek programa.


# Counting sort za sortiranje po bitih
# Ta funkcija izvede counting sort, da uredi vhodni vektor števil po določenem bitu (exp).
# Vhodni argumenti:
# - arr: Vektor 8-bitnih števil, ki ga želimo urediti.
# - exp: Število, ki predstavlja indeks bita, po katerem želimo sortirati števila.

# Binarni Radix sort
# Ta funkcija izvede binarni Radix sort, ki sortira vhodni vektor števil po vseh bitih.
# Vhodni argument:
# - arr: Vektor 8-bitnih števil, ki ga želimo urediti.
# Funkcija izvede Counting sort za vsak bit v številu bitov (8-bitna števila).
# Ta postopek razvrščanja se izvede za vsak bit od najmanj pomembnega do najbolj pomembnega.

